/**
 * @param {string} id 아이디
 * @param {string} content 내용
 * @param {boolean} finished 완료여부
 * @param {string} category 카테고리
 * @param {Array} tags 태그들 
 * returns 투두리스트
 **/
function Todo(아이디, 내용, 완료여부, 카테고리, 태그들) {
  return 아이디;
}

function Create(내용) {
  return 내용;
}
/**
 * ID를 제외한 모든 속성을 수정할 수 있다.
 * 특정 할 일의 특정 태그를 수정할 수 있다.
 * @ param {string} content
 * returns content를 두 번 출력해준다.
 **/
function Update(태그들) {}

/**
 * 특정 할 일의 모든 태그를 제거할 수 있다.
 * 모든 할 일을 제거할 수 있다.
 * 특정 할 일의 특정 태그를 삭제할 수 있다.
 * 특정 할 일의 모든 태그를 제거할 수 있다.
 **/
function Delete(아이디, 내용, 태그들) {
  return 내용;
}
